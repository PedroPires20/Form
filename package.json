{
  "name": "form",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "yarn lerna run dev --stream",
    "server": "yarn workspace @form/server",
    "client": "yarn workspace @form/client"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.5.1",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "ramda": "^0.27.2"
  }
}
